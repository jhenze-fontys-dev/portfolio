/* -----------------------------------------------------------------------------
 * MQTT data service for {{entityName}}
 *
 * Generated file – DO NOT EDIT DIRECTLY.
 *
 * Source template : dev/templates/codeTemplates/template.mqttDataService.hbs
 * Generated by    : dev/generators/generate.mqttServices.js (planned)
 *
 * To change this file:
 * - Update dev/config/metadataStructure.json (MQTT entities)
 * - Or update template.mqttDataService.hbs / the matching generator
 * -------------------------------------------------------------------------- */

import mqtt from 'mqtt';

const MQTT_URL = '{{mqttUrl}}';
const TOPIC = '{{mqttTopic}}';

const client = mqtt.connect(MQTT_URL);

client.on('connect', () => {
  console.log('✅ Connected to MQTT broker:', MQTT_URL);
  client.subscribe(TOPIC, err => {
    if (err) {
      console.error('❌ Failed to subscribe to topic:', TOPIC);
    }
  });
});

// Example publish helper
export function publish(payload) {
  client.publish(TOPIC, JSON.stringify(payload));
}

/* ---------------------------------------------------------------------------
 * End of generated MQTT data service for: {{entityName}}
 * ------------------------------------------------------------------------ */
