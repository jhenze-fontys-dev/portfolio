/* -----------------------------------------------------------------------------
 * External API data service for {{entityName}}
 *
 * Generated file – DO NOT EDIT DIRECTLY.
 *
 * Source template : dev/templates/codeTemplates/template.apiDataService.hbs
 * Generated by    : dev/generators/generate.apiServices.js (planned)
 *
 * To change this file:
 * - Update dev/config/metadataStructure.json (API entities)
 * - Or update template.apiDataService.hbs / the matching generator
 * -------------------------------------------------------------------------- */

import fetch from 'node-fetch';

const BASE_URL = '{{apiBaseUrl}}';

/**
 * Build a full URL for the resource.
 */
function buildUrl(path = '') {
  const trimmed = path.startsWith('/') ? path.slice(1) : path;
  return `${BASE_URL}/${trimmed}`;
}

export async function getAll() {
  const res = await fetch(buildUrl('{{apiResourcePath}}'));
  if (!res.ok) throw new Error('Failed to fetch {{entityName}} from API');
  return await res.json();
}

export async function getById(id) {
  const res = await fetch(buildUrl('{{apiResourcePath}}/' + id));
  if (!res.ok) {
    if (res.status === 404) return null;
    throw new Error('Failed to fetch {{entityName}} from API');
  }
  return await res.json();
}

// Optional: create/update/remove depend on external API capabilities

/* ---------------------------------------------------------------------------
 * End of generated API data service for: {{entityName}}
 * ------------------------------------------------------------------------ */
