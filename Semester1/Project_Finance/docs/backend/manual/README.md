
# Backend Manual API Notes (Placeholder)

This folder is reserved for **handwritten backend API documentation** that
should never be overwritten by generators.

Use `docs/backend/manual/` when you need to capture API details, examples, or
migration notes that are **not** derived from metadata or OpenAPI and must be
maintained manually.

In contrast:

- `docs/backend/generated/` is for machine‑generated artifacts and summaries.
- `docs/backend/` contains higher‑level conceptual backend documentation.

End of document — Backend Manual API Notes (Placeholder).
